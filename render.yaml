services:
  - type: web
    name: rag-chatbot
    runtime: python
    rootDir: rag-chatbot   # ← add this (your subfolder name)
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false